using OpenQA.Selenium;
using OpenQA.Selenium.Chrome;

using System;

namespace SeleniumParrkingConsole
{
    class Program
    {
        static void Main(string[] args)
        {
            ////Location of the web driver
            //IWebDriver driver = new ChromeDriver("C:/WebDriver/bin");
            ////Telling the driver which url to go to
            //driver.Navigate().GoToUrl(@"http://localhost:8081/");
            //Console.WriteLine("driver url: " + driver.Url);
            ////Writing what we wanna test
            //IWebElement iWebElement = driver.FindElement()
            
        }
    }
}
